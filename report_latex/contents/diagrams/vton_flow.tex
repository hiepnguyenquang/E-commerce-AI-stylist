\begin{figure}[H]
\centering
\begin{tikzpicture}[every node/.style={font=\small}]
    \node[diagram user, minimum width=7.4cm] (frontend) at (0,0) {
        Giao diện mở SSE và gửi yêu cầu VTON
    };
    \node[diagram service, minimum width=7.4cm] (route) at (0,-1.6) {
        Medusa kiểm tra yêu cầu, tạo bản ghi \code{vton_job}
    };

    \node[diagram note, minimum width=4.4cm] (local) at (-2.5,-3.4) {
        \textbf{CatVTON cục bộ}\\
        Tạo chuỗi tác vụ theo từng vùng trang phục
    };
    \node[diagram note, minimum width=4.4cm] (cloud) at (2.5,-3.4) {
        \textbf{FLUX.2 đám mây}\\
        Tạo một tác vụ với nhiều ảnh tham chiếu
    };

    \node[diagram queue, minimum width=7.4cm] (jobq) at (0,-5.2) {
        RabbitMQ: \code{ai_vision_jobs}
    };
    \node[diagram ai, minimum width=7.4cm] (worker) at (0,-6.8) {
        Dịch vụ AI xử lý ảnh và lưu tệp kết quả
    };
    \node[diagram queue, minimum width=7.4cm] (resultq) at (0,-8.4) {
        RabbitMQ: \code{ai_vision_results}
    };
    \node[diagram service, minimum width=7.4cm] (orchestrator) at (0,-10.0) {
        Medusa cập nhật trạng thái và điều phối bước tiếp theo
    };
    \node[diagram note, minimum width=7.4cm] (next) at (0,-11.6) {
        Nếu còn bước cục bộ, ảnh kết quả trở thành đầu vào của tác vụ kế tiếp
    };
    \node[diagram user, minimum width=7.4cm] (result) at (0,-13.2) {
        SSE gửi trạng thái và ảnh kết quả tới giao diện
    };

    \draw[diagram arrow] (frontend) -- (route);
    \draw[diagram arrow] (route) -- (local);
    \draw[diagram arrow] (route) -- (cloud);
    \draw[diagram async] (local) -- (jobq);
    \draw[diagram async] (cloud) -- (jobq);
    \draw[diagram async] (jobq) -- (worker);
    \draw[diagram async] (worker) -- (resultq);
    \draw[diagram async] (resultq) -- (orchestrator);
    \draw[diagram logical] (orchestrator) -- (next);
    \draw[diagram async] (next) -- (result);
    \draw[diagram logical] (next.west) -- ++(-12mm,0) |- (jobq.west);
\end{tikzpicture}
\caption{Luồng xử lý Virtual Try-On bất đồng bộ qua RabbitMQ và SSE}
\label{fig:vton-flow}
\end{figure}
